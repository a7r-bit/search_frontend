services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - API_URL=http://search.mingas.by/api
        - BASE_URL=http://search.mingas.by
    container_name: frontend
    ports:
      - "8080:80"
    networks:
      - appnet

networks:
  appnet:
    external: true
    name: appnet
